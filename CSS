 /* zerar os estilos */
 * {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
    text-decoration: none;
    font-family: sans-serif;
    font-size: 14px;
}

body {
    width: 100%;
    min-height: 100vh;
    background-color: rgb(243, 243, 243);
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
}

.instagram-wrapper {
    display: flex; /* o display flex faz o container ficar alinhado na horizontal*/
    align-items: center; /* centraliza todo conteúdo na vertical - eixo y */
    justify-content: start /* começa o alinhamento conteúdo horizontal pela esquerda - eixo x */;
    width: 60%;
    height: 100vh;
}

.instagram-phone {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 50%;
}

.instagram-phone img {
    height: 45rem;
}

.instagram-continue {
    display: flex;
    align-items: center; /* horizontal devido ao flex-direction column */
    justify-content: space-around; /* vertical devido ao flex-direction */
    flex-direction: column;
    width: 50%;
    height: 34rem;
}

.group {
    display: flex;
    align-items: center; /* horizontal */
    justify-content: space-between; /* vertical */
    flex-direction: column;
    background-color: #fff;
    width: 100%;
    padding: 1.3rem 0;
    border: 1px solid lightgray;
}

/* aplica-se ao primeiro grupo irmão (foto perfil) */
.group:nth-child(1) {
    min-height: 19rem;
}

.instagram-logo {
    height: 3rem;
}

.profile-photo {
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%; /* cria uma borda em forma de círculo em volta da foto */
    overflow: hidden; /* esconde as pontas que sobraram da foto quadrada dentro do círculo */
}

.profile-photo img {
    height: 6rem;
}

.instagram-login {
    background-color: #0095f6;
    color: #fff;
    padding: 8px;
    border-radius: 4px;
}

.instagram-logout {
    color: #0095f6;
    margin-top: 1rem;
}

.not-account {
    color: rgb(160, 160, 160);
}

.link-blue {
    color: #0095f6;
}

.get-the-app {
    display: flex;
    align-items: center; /* horizontal */
    justify-content: center; /* vertical */
    flex-direction: column;
    width: 100%;
    padding: 1.3rem 0;
}

.download {
    display: flex;
    width: 100%;
    justify-content: space-evenly; /* distribui os espaçamento exatamente igual entre os elementos para que fiquem centralizados*/
    align-items: center; /* por padrão nosso flex-direction está "row" = linha*/
    padding: 1rem;
}

.app-download {
    height: 3rem;
    width: 10rem;
    background-size: cover; /* ajusta a imagem na horizontal para ocupar o container mantendo a proporção*/
}

.app-download:nth-child(1) {
    background-image: url('./apple-button.png');
}

.app-download:nth-child(2) {
    background-image: url('./googleplay-button.png');
}

/* Mobile */

/*Tablet*/

@media (max-width: 1024px) {
    .instagram-wrapper {
        width: 90%;
    }
}

/*Celular*/

@media (max-width: 650px) {
    body {
        background-color: #fff;
    }

    .instagram-wrapper {
        width: 90%;
    }

    .instagram-phone {
        display: none;
    } /* esconde a imagem do celular, deixando apenas a janela para login */

    .instagram-continue {
        width: 100%;
    }

    .group {
        border: 1px solid transparent;
    }
}
